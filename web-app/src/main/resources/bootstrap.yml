spring:
  application:
    name: airline-client
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8002

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#security:
  #user:
    #name: guest
    #password: guest123
